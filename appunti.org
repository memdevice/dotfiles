Serve di separare le configurazioni personali da quelle generali: probabilmente meglio creare tre repo separati,
uno per EMACS LM KIT (BI), uno per le mie configurazioni personali di Emacs, uno per tutti gli altri dotfiles.

Serve di definire in blocchi separati e commentabili le configurazioni
per windows e per linux, oppure renderle condizionali (quando in windows etc.)

Serve gestire il custom-file.el tra linux e windows, per font e per dimensioni frame

creare famiglia di keybindings per funzioni "menizza" (C-c d space/minus/pipe/slash/CamelCase)
creare famiglia di keyb. per funzioni di insert, dove avere insert date e insert time (e firma, e fortun etc.)
